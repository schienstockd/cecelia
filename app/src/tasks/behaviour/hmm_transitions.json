{
  "task": "hmmTransitions",
  "fun_name": "behaviour.hmm_transitions",
  "label": "HMM transitions",
  "category": "Behaviour",
  "env": ["local"],
  "resource_pool": "cpu",
  "scope": "set",
  "requires": { "axes": ["T"] },
  "params": [
    {
      "key": "pops",
      "required": true,
      "requiredMessage": "Select at least one population",
      "label": "Populations",
      "type": "popSelection",
      "popScope": "tracks",
      "multiple": true,
      "acrossSegmentations": true,
      "default": [],
      "tip": "Track populations whose HMM states to read"
    },
    {
      "key": "colName",
      "label": "Name",
      "type": "text",
      "default": "default",
      "tip": "Suffix for the output column: live.cell.hmm.transitions.<name>"
    },
    {
      "key": "hmmStates",
      "required": true,
      "requiredMessage": "Select the state columns — run HMM states first",
      "label": "HMM states",
      "type": "labelPropsColsSelection",
      "multiple": true,
      "trimPrefix": "live.cell.hmm.state.",
      "hideInComposite": true,
      "default": [],
      "tip": "HMM state columns; several combine into a hybrid state"
    },
    {
      "key": "includeStart",
      "label": "Include start",
      "type": "bool",
      "default": false,
      "tip": "Emit the first in-track transition (from the track start)"
    },
    {
      "key": "includeSelfTransitions",
      "label": "Self transitions",
      "type": "bool",
      "default": true,
      "tip": "Keep transitions where the state does not change (e.g. 1_1)"
    }
  ]
}
