{
  "task": "driftCorrect",
  "fun_name": "cleanupImages.driftCorrect",
  "label": "Drift correction",
  "category": "Cleanup",
  "env": ["local"],
  "resource_pool": "cpu",
  "outputValueName": "driftCorrected",
  "requires": { "axes": ["T"] },
  "params": [
    {
      "key": "valueName",
      "label": "Image to correct",
      "type": "valueNameSelection",
      "default": "default",
      "field": "filepaths",
      "tip": "Select which image version to apply drift correction to"
    },
    {
      "key": "driftChannel",
      "label": "Drift reference channel",
      "type": "channelSelection",
      "multiple": false,
      "default": [],
      "tip": "Channel used for phase cross-correlation to estimate per-timepoint drift"
    },
    {
      "key": "driftEstimator",
      "label": "Estimator",
      "type": "select",
      "default": "multiLag",
      "options": [
        { "label": "Multi-lag",       "value": "multiLag" },
        { "label": "Chain",           "value": "chain" },
        { "label": "Rigid (SimpleITK)", "value": "sitkRigid" }
      ],
      "figure": "driftEstimator",
      "tip": "Multi-lag and chain correct translation only; rigid also fits in-plane rotation"
    },
    {
      "key": "driftMaxLag",
      "label": "Max frame gap",
      "type": "int",
      "default": 3,
      "min": 1,
      "max": 8,
      "step": 1,
      "showIf": { "driftEstimator": ["multiLag"] },
      "tip": "How far apart two frames may be and still be compared — higher is more robust but slower"
    },
    {
      "key": "driftMaxAngle",
      "label": "Max rotation per frame (°)",
      "type": "float",
      "default": 5.0,
      "min": 0.5,
      "max": 30.0,
      "step": 0.5,
      "showIf": { "driftEstimator": ["sitkRigid"] },
      "tip": "Above this, the frame's fit is discarded and its rotation is predicted from neighbours"
    }
  ]
}
