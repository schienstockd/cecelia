{
  "task": "tProject",
  "fun_name": "editImages.tProject",
  "label": "T-projection",
  "category": "Edit",
  "env": [
    "local"
  ],
  "resource_pool": "io",
  "outputValueName": "default",
  "requires": { "axes": ["T"] },
  "params": [
    {
      "key": "valueName",
      "label": "Source image",
      "type": "valueNameSelection",
      "default": "default",
      "field": "filepaths",
      "tip": "Which image version to project"
    },
    {
      "key": "op",
      "label": "Projection",
      "type": "select",
      "default": "mean",
      "options": [
        { "label": "Max",    "value": "max",    "help": "Brightest voxel per pixel across the whole timelapse" },
        { "label": "Mean",   "value": "mean",   "help": "Average — a stable background from a long acquisition" },
        { "label": "Median", "value": "median", "help": "Robust average; slower — rechunks T into one block" },
        { "label": "Sum",    "value": "sum",    "help": "Photon-count math; can saturate an integer dtype" },
        { "label": "Min",    "value": "min",    "help": "Dimmest voxel per pixel across time" },
        { "label": "Std",    "value": "std",    "help": "Where signal varies through time — a motion / activity map" }
      ],
      "tip": "Which statistic collapses the T axis"
    }
  ]
}
