{
  "task": "branching",
  "fun_name": "segment.branching",
  "label": "Branching (skeleton) analysis",
  "category": "Segment",
  "env": ["local"],
  "resource_pool": "cpu",
  "params": [
    {
      "key": "valueName",
      "label": "Segmentation",
      "type": "valueNameSelection",
      "field": "labels",
      "default": "default",
      "tip": "Select which label set to skeletonise"
    },
    {
      "key": "outputValueName",
      "label": "Output name",
      "type": "valueNameInput",
      "namespace": "labels",
      "default": "default",
      "tip": "Name for the output branch label set"
    },
    {
      "key": "refPops",
      "label": "Restrict to population",
      "type": "popSelection",
      "accepts": ["live", "clust", "region"],
      "default": "NONE",
      "tip": "Skeletonise only these cells; NONE = the whole segmentation"
    },
    {
      "key": "preDilationSize",
      "label": "Pre-dilation",
      "type": "int",
      "min": 0,
      "max": 10,
      "step": 1,
      "default": 2,
      "tip": "Close gaps by this many pixels before skeletonising (0 = off)"
    },
    {
      "key": "postDilationSize",
      "label": "Post-dilation",
      "type": "int",
      "min": 0,
      "max": 10,
      "step": 1,
      "default": 2,
      "tip": "Thicken the saved skeleton for visibility; measures are unaffected"
    },
    {
      "key": "useBorders",
      "label": "Use label boundaries",
      "type": "bool",
      "default": false,
      "tip": "Skeletonise the label outlines instead of their interiors"
    },
    {
      "key": "flattenBranching",
      "label": "Flatten Z",
      "type": "bool",
      "default": false,
      "tip": "Project the stack to one plane and skeletonise in 2D (3D only)"
    },
    {
      "key": "integrateTime",
      "tip": "One network for the whole movie",
      "label": "Integrate time",
      "type": "bool",
      "default": false
    },
    {
      "key": "integrateTimeMode",
      "tip": "Applies to the raw channel; labels always merge",
      "label": "Integrate mode",
      "type": "select",
      "default": "max",
      "options": [
        { "value": "max", "label": "max" },
        { "value": "avg", "label": "avg" }
      ]
    },
    {
      "key": "calcAnisotropy",
      "label": "Anisotropy",
      "type": "bool",
      "default": false,
      "tip": "Also compute the fibre-orientation field and per-image anisotropy"
    },
    {
      "key": "calcFlattened",
      "label": "Anisotropy on Z-MIP",
      "type": "bool",
      "default": false,
      "tip": "Measure alignment on the Z projection instead of the full stack"
    },
    {
      "key": "anisotropySource",
      "tip": "Channel survives a bad segmentation; skeleton is smoother",
      "label": "Anisotropy from",
      "type": "select",
      "default": "skeleton",
      "options": [
        { "value": "skeleton", "label": "skeleton" },
        { "value": "mask",     "label": "mask" },
        { "value": "channel",  "label": "channel" }
      ]
    },
    {
      "key": "fibreChannels",
      "label": "Fibre channels",
      "type": "channelSelection",
      "multiple": true,
      "default": [],
      "tip": "Only used when Anisotropy from = channel"
    },
    {
      "key": "structureTensorSigmaUm",
      "label": "Smoothing scale (µm)",
      "type": "float",
      "min": 0.5,
      "max": 50.0,
      "step": 0.5,
      "default": 7.0,
      "tip": "Start ~7 µm, near fibre thickness; too low reads noise as structure"
    },
    {
      "key": "anisotropyBoxUm",
      "label": "Grid spacing (µm)",
      "type": "float",
      "min": 0.5,
      "max": 100.0,
      "step": 0.5,
      "default": 5.0,
      "tip": "Start ~5 µm; finer is better, but halving it quadruples the file"
    }
  ]
}
