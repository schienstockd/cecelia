{
  "task": "trainSupportDenoise",
  "fun_name": "opticalFlow.trainSupportDenoise",
  "label": "Train denoise model (SUPPORT)",
  "category": "Model training",
  "env": ["local"],
  "resource_pool": "gpu",
  "scope": "set",
  "requires": { "axes": ["T"], "scale": ["xy", "t"] },
  "params": [
    {
      "key": "valueName",
      "label": "Images to train on",
      "type": "valueNameSelection",
      "default": "driftCorrected",
      "tip": "Use the drift-corrected version — SUPPORT compares the same pixel across frames"
    },
    {
      "key": "modelName",
      "optionsFrom": "denoiseModels",
      "label": "Model name",
      "type": "valueNameInput",
      "namespace": "models",
      "default": "",
      "tip": "Saved to the denoise vault under this name"
    },
    {
      "key": "trainChannels",
      "label": "Channels",
      "type": "channelSelection",
      "multiple": true,
      "default": [],
      "tip": "Pool channels into one model — one training run per set"
    },
    {
      "key": "unetSize",
      "label": "Model size",
      "type": "select",
      "variant": "chips",
      "default": "medium",
      "tip": "Bigger = better results but slower and more VRAM",
      "options": [
        { "label": "Small",  "value": "small",  "help": "Fast; fits any GPU" },
        { "label": "Medium", "value": "medium", "help": "Balanced quality; needs ~4 GB free VRAM" },
        { "label": "Large",  "value": "large",  "help": "Best quality; needs ~5 GB free VRAM, slower to train" }
      ]
    },
    {
      "key": "inputFrames",
      "label": "Temporal window",
      "type": "int",
      "min": 11,
      "max": 121,
      "step": 2,
      "default": 61,
      "tip": "How many frames of context (odd, ≤ movie length)"
    },
    {
      "key": "patchXY",
      "label": "Patch size",
      "type": "int",
      "min": 64,
      "max": 256,
      "step": 32,
      "default": 128,
      "tip": "Spatial crop per training sample — bigger keeps context, costs more VRAM"
    },
    {
      "key": "epochs",
      "label": "Epochs",
      "type": "int",
      "min": 1,
      "max": 100,
      "step": 1,
      "default": 20,
      "tip": "Training passes over the pooled patches"
    },
    {
      "key": "midZOnly",
      "label": "Middle Z plane only",
      "type": "bool",
      "default": true,
      "tip": "Train on the middle Z per movie — matches per-Z inference"
    },
    {
      "key": "overwrite",
      "label": "Overwrite existing",
      "type": "bool",
      "default": false,
      "tip": "Replace a model of the same name in the vault"
    },
    {
      "key": "advanced",
      "label": "Advanced",
      "type": "section",
      "collapsed": true,
      "params": [
        {
          "key": "batchSize",
          "label": "Batch size",
          "type": "int",
          "min": 1,
          "max": 8,
          "step": 1,
          "default": 2,
          "tip": "Patches per gradient step — lower if you hit CUDA OOM"
        },
        {
          "key": "learningRate",
          "label": "Learning rate",
          "type": "float",
          "min": 0.0001,
          "max": 0.01,
          "step": 0.0001,
          "default": 0.0005,
          "tip": "How fast the model learns — lower is safer"
        },
        {
          "key": "blindConvChannels",
          "label": "Blind-spot channels",
          "type": "int",
          "min": 16,
          "max": 128,
          "step": 16,
          "default": 64,
          "tip": "Capacity of the denoising branch — leave at 64"
        }
      ]
    }
  ]
}
